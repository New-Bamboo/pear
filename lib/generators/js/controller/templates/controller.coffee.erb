define ['vendor/controller', 'vendor/template!<%= underscore_name %>'], (Controller, template) ->
  
  class <%= controller_name.camelize %> extends Controller

    createDOM: -> super.addClass('<%= controller_name.dasherize %>')

    @after 'init', ->
      @render()

    render: ->
      $(@dom).html template(greeting: 'Hello!')
